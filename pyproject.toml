[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "raven-py"
version = "0.8.0"
description = "RAVEN: Retrieval-Aware Vector Engine with Navigation"
license = "Apache-2.0"
requires-python = ">=3.8"

[tool.maturin]
features = ["python"]
